# Optional: force backend URL instead of automatic host-based routing.
VITE_BACKEND_BASE_URL=https://api-autonomous-spacecraft.demo.sparkup.dev

# Optional: override SparkUp static assets base URL.
VITE_SPARKUP_STATIC_BASE=https://static.sparkup.dev

# Optional: top-left logo link target.
VITE_SPARKUP_PORTFOLIO_URL=https://www.sparkup.dev

# Optional: repository link used in navbar and homepage.
VITE_REPO_URL=https://github.com/sparkup/autonomous-spacecraft-rl